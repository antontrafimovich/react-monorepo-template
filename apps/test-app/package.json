{
  "name": "test-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@common/ui-kit": "1.0.0"
  },
  "scripts": {
    "start": "react-app-rewired start",
    "build": "eact-app-rewired build",
    "test": "eact-app-rewired test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
